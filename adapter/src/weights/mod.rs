//! Weight container adapters (G163, ADR 0078): format mechanics -- reading a container into the
//! physical census `atlas_core::weights` defines, and constructing a container from one.

pub mod safetensors;
