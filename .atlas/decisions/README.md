---
id: atlas.decisions.index
type: reference
status: active
canonical: true
---
# Atlas Decisions

Only durable architecture choices belong here. Temporary implementation history, migration notes and progress reports stay in Git history or non-canonical evidence.

A superseded decision must identify its successor. No historical fleet/mirror decision is currently canonical.

## Accepted decisions

- `0001-one-normalized-semantic-path.md` — all semantic producers converge through census → normalize → reconcile before graph/ATLAS projection.
- `0002-epistemic-status-model.md` — one canonical EpistemicStatus vocabulary; FactKind, EvidenceKind and Disposition remain separate.
- `0003-constraint-derivation-provenance.md` — Souffle's provenance pattern absorbed as an additive `ConstraintResult.derivation` field, projected into the engineering graph via `add_constraint_derivations`.
- `0004-semi-naive-dependency-closure.md` — datafrog's semi-naive delta-driven fixed point absorbed as `core::closure`, computing origin-labelled transitive dependency reachability in `census_cargo_workspace` (`DependencyClosureReport.reachability`, schema v4).
- `0005-native-blake3-content-digest.md` — BLAKE3's unkeyed hash mode absorbed natively as `core::identity::blake3`; every inventoried regular file carries a validated `blake3-256:` `ArtifactRecord.content_digest` from one bounded, verified read (inventory schema v2). Supersedes the genome record's native-port rejection for the unkeyed mode only.
- `0006-inventory-change-detection.md` — rust-analyzer vfs's Create/Modify/Delete taxonomy absorbed as `core::census::delta::diff_inventories` over BLAKE3 content identity; `systemize --previous` emits `inventory_delta` (report schema v13).
- `0007-three-valued-constraint-verdict.md` — `ConstraintVerdict { Satisfied, Violated, Unknown }` (strong Kleene conjunction) on every ADL constraint result; an undeclared required attribute is UNKNOWN (`ATLAS-E055`), not a violation; admission blocks on `ADL_CONSTRAINT_VIOLATED` and `ADL_CONSTRAINT_UNKNOWN` separately.
- `0008-extraction-derivation-cache.md` — persisted per-artifact `ExtractionBatch` memo keyed on BLAKE3 over the full extraction input, the digest of the bytes read, and a build digest of all extractor/core sources + `Cargo.lock`; used only when read bytes equal the inventoried digest; `systemize --cache`. Measured: reports byte-identical, but graph summarization (~95% of run time) — not extraction — is the recensus cost centre.
- `0009-indexed-graph-node-dedup.md` — `ensure_node` deduplicates through an incremental, non-serialized node-id index instead of a linear scan (append-only contract, shrink rebuild, debug sentinel); graph output byte-identical, `systemize` 187 s → 12.9 s (debug).
- `0010-physical-quantities-and-dimensional-constraints.md` — `core::quantity`: exact rational SI quantities over 8 base dimensions (angle separate); units with irrational/affine factors refused; ADL `require x.attr (==|>=|<=|>|<) <quantity>` compares by dimension and exact value (E056 dimension mismatch = VIOLATED, E057 unsupported unit = UNKNOWN, E058 ordering over non-quantity = UNKNOWN); 75/75 units agree with pint.
- `0011-visual-observation-record.md` — Creator Fabric's first primitive: hermetic browser observation (`atlas-systemizer observe`) → OBSERVED element geometry/style → DERIVED ratio-first layout relations with resolution-bounded uncertainty and interval-stated responsive rules; `contracts/CREATOR-FABRIC.md` fixes authorization/originality/epistemic rules.
- `0012-planar-arm-physical-model.md` — physical milestone 1: `core::physical::PlanarArm` from ADL Arm/Link/Joint entities with dimension-checked quantities; exact reach, inner radius and worst-case static shoulder torque (DERIVED, assumptions stated); requirement verdicts; FK verified vs closed form; `atlas-systemizer physical`; `contracts/PHYSICAL-ENGINEERING.md`.
- `0013-browser-as-instrument-breakpoint-bisection.md` — `observe --bisect`: responsive changes located to one pixel by re-observation (INFERRED, monotone-transition assumption stated); the fixture's `max-width: 699px` measured at exactly (699, 700) in 10 probes.
- `0014-cross-domain-drivetrain-power.md` — physical milestone 2: joint torque → motor torque (gear, efficiency) → holding current (k_t) → heat (I²R) → rail total and power, all exact and dimension-checked, every value naming its inputs; a heavier payload that keeps reach SATISFIED is caught VIOLATED at the power rail.
- `0015-interaction-stimuli-and-mechanisms.md` — `observe --interact`: hover/click/click-twice/focus stimuli in fresh hermetic contexts, settled after animations, diffed against baseline → OBSERVED changes → DERIVED mechanisms (HOVER_AFFORDANCE, DISCLOSURE with controlled elements, TOGGLE, FOCUS_INDICATOR) and a state graph.
- `0016-motion-curve-inference.md` — `observe --motion`: paused/seeked deterministic curve sampling of every hover animation; easing INFERRED from the curve alone against the CSS keyword curves (alternatives + residuals, overshoot flag), validated against the engine's declared timing (5/5 agree) and Chromium-sampled keyword values.
- `0017-creator-construction-loop.md` — `atlas-systemizer create`: ratio-first DesignIntent → deterministic placeholder-only HTML/CSS → rendered → re-observed through layout/bisection/stimuli/motion → every intended relation SATISFIED/VIOLATED/UNKNOWN; an original intent verifies 10/10, a tampered artifact fails exactly the tampered relations.
- `0018-design-genome-and-recombination.md` — `genome`: mechanisms (SPLIT_HERO, COLLAPSING_GRID, HOVER_LIFT) abstracted from observed references as small rational ratios with provenance; `create --genome --recipe`: recombination refused unless it draws on ≥2 sources and varies every mechanism, each parameter traced to inherited/varied/default, then created and verified.
- `0019-arm-dynamics-and-simulation.md` — ADR 0002 gains SIMULATED; closed-form 2R Lagrangian dynamics, minimum-jerk declared trajectories, computed-torque RK4 simulation with exact BLAKE3 run identity and a divergence guard; actuator capability decided on DERIVED inverse-dynamics demand, tracking on SIMULATED evidence; a fast move that passes every static check is caught.
- `0020-design-search-and-pareto-preservation.md` — `search`: deterministic originality-valid candidates over a genome, each created and verified; measured objectives with stated meanings (novelty, chars-per-line proxy, narrow content extent); only verified, fully measured candidates compete; the whole non-dominated set is kept, never a single "best".
- `0021-bounded-donor-working-set.md` — donor source is temporary working material: storage state orthogonal to decision state (11 states, presence rules), cheapest-sufficient materialization modes, measured disk watermarks + budget (available − build headroom − safety reserve) + working-set cap gate every materialization; accounted dependencies are not cloned; storage integrity enforced; clone-first superseded.
- `0022-browser-instrument-abstraction.md` — browser engines are replaceable instruments: `BrowserInstrument` (capabilities, explicit availability/unsupported), instrument-declared layout resolution and network policy, W3C WebDriver backend (chromedriver verified, Ladybird harness-ready but build-blocked), `EngineDifferentialReport` (layout/breakpoints/interactions, unclassified until investigated, same-engine never reported as independent), `CREATOR-INSTRUMENTS.toml` registry + promotion gate.
- `0023-product-semantic-ir-and-unit-economics.md` — PRODUCT_FOUNDRY first slice: ADL-declared Product IR (hypothesis, variant, SKU, BOM, cost items/rates, price, quote, requirement), exact money intervals with an evidence basis on every value, dependency-aware unit economics to break-even, interval-decided requirements; estimates without uncertainty/source refused, QUOTED requires a Quote, UNKNOWN never zero, no silent currency conversion; oracle-exact on the desk-organizer fixture.
- `0024-self-recensus-proof-of-nextgen.md` — PRIORITY RESET: P0 census closure .. P6 construction canonical (PRIORITY.toml), secondary families frozen; self-recensus (revision-independent CensusSnapshot, intent-vs-observation proof with forbidden regressions and deterministic replay) mandatory and chained for every generation from G57.
- `0025-census-certificate.md` — CensusCertificate v2 computed from the census (schema-conformant, typed blockers, state follows blockers); self-scope CENSUSED: provenance incomplete (230 facts w/o revision, 120 w/o extractor), atlas-cli workspace member outside inventory, single-engine extraction, 8 UNKNOWN coverage dimensions, no .atlas root.
- `0026-adl-consumes-census-truth.md` — exit criterion C: authored `depends_on` reconciled against the Cargo dependency census by materialization path (SATISFIED / ATLAS-E060 / E061 / E062; uncheckable relations are explicit deltas); census truth the authored ADL lacks is derived as `.atlas/declared/census.adl` with a drift gate.
- `0027-minimum-atlas-census-container.md` — exit criterion D: ATLAS wire v1 writer/reader/validator for an explicitly unsealed census container (facts, obligations, declared ADL graph, certificate); pinned varint/wire-type/flag/schema-id/root-identity encodings; transactional publication; the certificate names the verified root as ATLAS_ROOT_UNSEALED, never SEALED.
- `0028-revision-stable-function-identity.md` — first-50 #7 SCIP: measured identity loss (revision and span in FunctionIdentity keys, file-granular recensus); absorbed position-free descriptors natively; Atlas-native correspondence (SAME/CHANGED/RENAMED/MOVED/MOVED_RENAMED/DELETED/CREATED/AMBIGUOUS, never forced); snapshot v3 makes entity changes part of the proof.
- `0029-identity-carries-location.md` — first-50 #8 Kythe: 203 symbol/type ids conflated definitions across files (graph dropped 609 nodes); absorbed VName `path` as an identity field (unresolved spellings file-scoped, canonical types shared); corpus/root/language/Entry store REFERENCE_ONLY.
- `0030-definition-derived-schema-identity.md` — first-50 #9 Glean: the G64 name-derived schema_id let a tag-repurposing version be silently misread (measured); schema_id is now derived from declared record schemas (fields by name/tag/wire/requiredness + dependencies), which writer and reader address by name; Angle/fact DB/query IR REFERENCE_ONLY.
- `0031-native-rust-path-resolution.md` — first-50 #4 rust-analyzer: hir-def name resolution absorbed natively for path calls (module tree, item scopes, import fixed point, block scopes, extern prelude, inherent/trait-impl lookup; never a guess); a second CALL engine observing the syntactic claims with STATIC_RESOLVED callees (3,731/3,731 confirmed by the pinned SCIP oracle, 95.1% recall); per-dimension multi-engine blocker; hir-ty inference REFERENCE_ONLY.
- `0032-effects-from-resolved-std-paths.md` — G77 (P0, miri-cycle input): name resolution spells canonical std paths through imports; a declared std-path effect table (std::fs entry points) turns 323 resolved filesystem calls into DERIVED FILESYSTEM_READ/WRITE effect sites of their callers (all confirmed by SCIP); env/time/process-spawn stay unobserved, never mislabeled.
- `0033-self-receiver-method-resolution.md` — G79 (P0, clef-cycle input): `self.m()` resolved to the unique inherent method of the impl's self type when receiver forms and impl shape match (the method probe's first step); 496 calls, all SCIP-confirmed; other method calls wait for type inference.
- `0034-canonical-type-identity.md` — G83 (P0, egglog-cycle input): type occurrences canonicalized bottom-up through name resolution (workspace descriptors, std paths, structural composition; never a guess); 1,723 DERIVED canonical claims unite 176 spellings of 84 types, 3,077 occurrences SCIP-confirmed with 0 disagreements.
- `0035-schema-evolution-conformance.md` — first-50 #22 FlatBuffers: `flatc --conform` absorbed as `.atlas` schema conformance (keep name/dependencies/kinds, fields keep tag and wire type, never removed or tightened, additions optional); recorded definition history; the reader accepts every recorded conforming identity; vtables/flexbuffers/reflection REFERENCE_ONLY.
- `0036-ambient-input-effects.md` — first-50 #27 IRIS: its split of ambient inputs (EnvGet, ClockNs) from I/O absorbed as `ENVIRONMENT_READ`/`CLOCK_READ`; the std-path effect table declares `std::env` reads and `Instant::now`/`SystemTime::now`, turning the 59 resolved env/time calls ADR 0032 left unobserved into DERIVED effect sites (59/59 SCIP-confirmed); environment writes stay undeclared.
- `0037-generation-product-match.md` — first-50 #45 in-toto: the MATCH rule on products absorbed into the self-recensus chain; a generation's proven post-change census must equal its committed tree's census (clean-HEAD `atlas.json`), required for every generation since `atlas pack` existed; 44/44 match; layouts and signed links REFERENCE_ONLY.
- `0038-essential-complexity-audit.md` — G116 retrospective audit: essential complexity is debt, not a donor verdict; `contracts/ESSENTIAL-COMPLEXITY.md` (ESSENTIAL / SCALE_TRIGGERED / OPTIONAL, anti-avoidance, Rules A and B), `roadmap/ESSENTIAL-COMPLEXITY-DEBT.toml` (39 debts, alarms REVIEW > 12 / ESCALATION > 24 stale generations, skip budget 3, exit E as a finite graph, ordered native attack queue), 50 decisions re-audited, FIRST_50_CAMPAIGN_COMPLETE ≠ FOUNDATIONAL_ATLAS_READY.
- `0039-concurrency-from-resolved-std-paths.md` — G117 (P0, native attack queue head): a declared std-path concurrency table (`std::thread::scope` → new `THREAD_SCOPE`, `std::thread::spawn` → `SPAWN`, `mpsc` constructors → `CHANNEL_CREATE`); the path-resolution engine derives CONCURRENCY sites of their callers; the falsified zero becomes 2 DERIVED thread scopes, SCIP-confirmed 2/2; CONCURRENCY leaves the multi-engine blocker; methods and closure bodies stay outside (queued).
- `0040-hard-stop-anti-avoidance.md` — G118 hard stop: "remove accidental complexity aggressively, conquer essential complexity deliberately" made a CI property; 63-donor capability audit (58 corpus + 5 First-50 never admitted) with separate mechanism and capability decisions; debt ledger v2 (41 debts, 15 dimensions, construction ownership, P4 numeric thresholds, drift); escalation TRACK/REVIEW 13/ESCALATION 25/BLOCK 32; pressure map; FOUNDATIONAL-ATLAS-READY gate with donor-progression and frontier stop; 19 enforcement tests.
- `0041-call-macro-argument-recovery.md` — G119 (P0, the mandatory native attack after the hard stop): 20 standard expression macros re-parsed (never expanded), inert macros and local shadowing honoured; per-file CALL coverage OBSERVED where nothing is opaque (75 of 98 files), proven against an independent enumeration of every workspace source; +4,745 call sites; scope coverage aggregation fixed from best-of to worst-artifact.
- `0042-role-aware-macro-arguments.md` — G120: standard macro arguments carry each dimension's own semantics (Evaluated / Formatted / Compared / WriteTarget / FormatString roles; implicit format captures at exact columns; assert family as INFERRED panic sites and statement-level decision points); opaque residuals named per dimension; scope aggregation tested adversarially for every dimension; generation metrics derived from census snapshots (G119 metric corrected).
- `0043-recursive-donor-revalidation.md` — G121: historical donor verdicts are valid only for the capabilities that produced them; capability-based REVALIDATION_REQUIRED triggers for every audited donor; HISTORICAL_DONOR_REVALIDATION distinct from (still blocked) new-donor progression; R1 rust re-materialized at its exact pin, censused by the G73 and current engines, REVALIDATED with a corrected reason, deleted again.
- `0044-agent-worn-atlas-semantic-composition.md` — G122: DEBT-SEMANTIC-COMPOSITION (essential, P0) and its first attack: census records composed into function, component, subsystem, capability and architecture models with typed relations (INVOKES / SUPPLIES_DATA / STATE_FLOW, never causal), typed invariants (dependency OBSERVED from Cargo and falsifiable by a counterexample call; state and authority INFERRED with residuals) and routed understanding gaps; `atlas-systemizer agent` (understand → MissionContext, explain, impact, trace, why, hypothesis, verify, ...); composed totals recensused; generation classes with bounded interleaving and an agent-mission cadence; DEBT_TRIGGERED_DONOR_ATTACK defined.
- `0045-quantity-kinds-and-intervals-in-core.md` — G124 (native attack queue head NA-QUANTITY-IN-CORE, worked through the Agent-Worn interface): QuantityKind (energy, torque, frequency, activity) named by unit or declared, never combined across kinds; exact interval uncertainty propagated through arithmetic (containment property test), overlapping comparisons UNDECIDED, marked f64 approximations; ADL kind mismatch VIOLATED (ATLAS-E059); declared quantities become QUANTITY census facts and graph Quantity nodes; physical torques typed.
- `0046-persistence-from-resolved-std-paths.md` — G125 (native attack queue head NA-PERSISTENCE-RESOLVED, via the Agent-Worn interface): a declared std-path persistence table (fs write/copy DURABLE_WRITE, read/read_to_string DURABLE_READ, File sync_all/sync_data SYNC); the path-resolution engine derives DERIVED persistence sites with the resolved API as evidence; 129 of 129 SCIP-confirmed; PERSISTENCE leaves the multi-engine blocker; spelling guesses stay INFERRED.
- `0047-restricted-subprocess-sandbox.md` — G126 (queue head NA-SANDBOX-SUBPROCESS, construction M16): SandboxRequest/SandboxRun records; a restricted-subprocess backend with digest-verified staged inputs, cleared environment, confined working directory, closed stdin, process-group timeout and probed network namespace; every isolation property recorded ENFORCED or UNENFORCED; filesystem confinement never claimed (no unsafe code in Atlas); `sandbox probe`.
- `0048-verification-obligations-and-evidence.md` — G127 (queue head NA-VERIFICATION-EVIDENCE, construction node M2): typed verification obligations, evidence, reports and failures; state from the evidence admissible for the exact candidate (none -> UNVERIFIED, any violation -> FAILED); library-package policy (SEMANTIC, UNIT, COMPATIBILITY); the self scope evaluated against evidence bound to its census digest (`verification self`); M2 EXISTS.
- `0049-affected-impact-closure-with-oracle.md` — G129 (queue head NA-IMPACT-CLOSURE): the affected-impact closure of a change (seed, callees, callers of removed functions, name candidates of added ones; kind-specific invariant rules; workspace manifests and the system ADL global) checked by a full-recompute oracle over revision-normalized world models; SOUND on seven recorded changes; `agent closure`; DEBT-IMPACT_CLOSURE M3.
- `0050-census-quantified-invariants.md` — G130 (queue head NA-CONSTRAINT-NONGROUND): ADL `forall f: function in <Entity> forbid effect <C>` / `forbid call to <Entity>` quantify over census truth; the ADL compiler alone reports UNKNOWN (ATLAS-E063); systemize decides them over the composed census (VIOLATED with named counterexamples, ATLAS-E064; SATISFIED only with no unexamined residual; UNKNOWN naming it) in one step shared by every entry point; Atlas declares its six-rule layering, all SATISFIED; DEBT-CONSTRAINT_REASONING M3.
- `0051-one-uncertainty-carrier-and-marked-float-drops.md` — G131 (queue head NA-UNCERTAINTY-PREDICTIONS): `quantity::Interval` is the one exact uncertainty carrier (quantity bounds and product money; sign-aware arithmetic); `FloatDrop` records every value that leaves exact arithmetic; physical verdicts honor declared uncertainty (exact joint limits, interval actuator capability, UNKNOWN when uncertain inputs feed a nominal simulation); 13 drops recorded per 2-link simulation.
- `0052-closure-regions.md` — G133 (queue head NA-CLOSURE-REGIONS): a closure is its own executable region (CLOSURE FunctionIdentity `{closure@line:column}` under `fn <enclosing region>`); every dimension builder runs on its body, attributed to it, never to the enclosing function; the path-resolution engine walks closure bodies with captured plus own locals; composition links each region to its enclosing one; `async` blocks stay outside.
- `0053-one-epistemic-vocabulary.md` — G134 (queue head NA-EPISTEMIC-UNIFICATION): `core::vocabulary` maps every status/verdict/outcome/state/level carrier by role, lists boundary text and name-only collisions; a test rejects any workspace struct field outside the map; the graph's float confidence becomes the fact's EpistemicStatus; product hypothesis status, physical evidence level and six verdict/outcome strings become typed (wire-compatible); `.atlas` records with a status outside the vocabulary are refused and rejected; futures states decided; SIMULATED joins SEMANTIC-FACTS.
- `0054-typed-relations.md` — G135 (queue head NA-TYPED-RELATIONS): `EdgeKind` is the closed, wire-compatible set of 35 graph relations, each with category, cardinality and ownership; declared ADL relations are `depends_on`, `provides`, `contains`, and any other name is rejected (ATLAS-E065) and states no edge; no kind is causal until a counterfactual record exists; DEBT-CAUSALITY M0 -> M2.
- `0055-effect-envelopes-reconciled-with-census.md` — G137 (queue head NA-ADL-BEYOND-DEPENDENCIES): `forall f: function in <Entity> observed effect within <C>, ...` (or `none`) reconciles an entity's declared effect envelope with its definite effect sites — VIOLATED naming each site outside it, else SATISFIED naming the INFERRED sites and unobserved files it does not cover; `adl derive` lowers every unbounded subsystem's envelope into `census.adl` (categories only, a fixed point); a new category against the committed envelope blocks admission and drifts the derivation.
- `0056-architectural-integrity-envelope-and-report.md` — G138 (queue head NA-INTEGRITY-ENVELOPE): `core::integrity` lowers census-falsifiable declarations (layering calls, effect bounds, Cargo-observable dependencies) into a pinned `ArchitecturalIntegrityEnvelope` (identity = digest of its invariants; `.atlas/declared/integrity-envelope.json`) and evaluates it into an `ArchitecturalIntegrityReport` (PASS/FAIL/UNKNOWN per invariant from decided results; a dropped pinned invariant is ARCHITECTURE_UNSELECTED_REVISION; counts and verdict re-derived); `integrity report` exits INTEGRITY_NOT_ELIGIBLE unless ELIGIBLE; Atlas ELIGIBLE with 14 HARD invariants.
- `0057-typed-local-method-resolution.md` — G139 (queue head NA-CALL-TYPE-RESIDUAL, first step): `x.m()` on a parameter or top-level `let` whose declared type is a plain workspace type (`T`, `&T`, `&mut T`, `Self` of a plain impl), bound once in the function, is decided by the G79 method-probe first step (the inherent method with the equal receiver form); 61 new resolutions on the self census, all agreeing with rust-analyzer SCIP; untyped lets, fields, chains and generic receivers stay unclaimed.
- `0058-trait-bound-receivers-dynamic-dispatch.md` — G140 (NA-CALL-TYPE-RESIDUAL step two): `x.m()` on a receiver known only by workspace-trait bounds (`&dyn Tr`, `impl Tr`, a function generic `T: Tr` inline or in a where clause, `self` in a trait default body) is DYNAMIC_PARTIAL with the trait method declaration as its one callee, when exactly one bound method has the caller's receiver form; foreign bounds, supertraits, impl-level generics and `Box<dyn>` stay unclaimed; 21 on the self census, all agreeing with SCIP.
- `0059-signature-typed-lets-and-guarded-autoref.md` — G142 (NA-CALL-TYPE-RESIDUAL step three): a top-level `let` bound once to a call whose resolved callee declares a plain workspace output (`Self` of a plain impl included), a struct literal or a tuple-struct/variant constructor types its local by value; the probe's autoref step (value → `&self`/`&mut self`, `&mut T` → `&self`) is claimed only when no by-value method of the name can come first (no by-value workspace trait method, not `into`/`try_into`/`into_iter`, no non-std import or open scope, no Iterator-like impl on the type); 174 new resolutions on the self census, all agreeing with SCIP.
- `0060-field-and-call-result-receivers.md` — G143 (NA-CALL-TYPE-RESIDUAL step four): one `receiver_type` gives a receiver expression's type as the probe sees it -- `self`, a typed local, a named field of a non-generic struct (`T`, `&T`, `&mut T`), a resolved call's plain declared output, parentheses -- and `method_outcome` applies the G79–G142 probe rules to every method call; lets bound to fields or call results are typed in order; 76 new resolutions on the self census, all agreeing with SCIP.
- `0061-std-receivers-and-method-level-persistence.md` — G144: a std receiver (a declared std type, generic arguments allowed, or a declared std constructor's output, `?` unwrapping a fallible one) calls a declared inherent std method (`File::sync_all`/`sync_data`, `Mutex::lock`, `Sender::send`, `Receiver::recv`) as its std path, read by the effect/persistence/concurrency tables; lets in nested blocks are typed within their block; the `.atlas` container writer's two fsync sites become DERIVED SYNC persistence (SCIP: `fs/impl#[File]sync_all`).
- `0062-bounded-item-macro-names-and-construction-reachability.md` — G145: an item-position invocation of a workspace `macro_rules!` (textual scope or `crate::name!`) may define only the names after item keywords in its transcribers (the invocation's identifiers where a metavariable fills one); only those lookups stay uncertain, glob imports carry them, and imports, non-expression macros or unlisted attributes keep the module open; 1,250 more type occurrences canonicalized, 15 path calls resolved (SCIP-agreeing); queue selection requires a construction-frontier node for the primary debt, and M5/M6 are recorded EXISTS.
- `0063-typed-records-in-the-census-container.md` — G147 (NA-ATLAS-TYPED-SECTIONS, construction node M1): SEMANTIC_RECORDS gains a declared record kind per typed family (header + embedded subject kinds 100+, RECORD/PACKED/BOOL wire types, shape suffixes that leave earlier schema ids unchanged), EVIDENCE and DIAGNOSTICS sections, FORMAT_MINOR 1; a schema-directed codec refuses undeclared values; typed records canonical by serde form, evidence references must resolve; atlas pack round-trips every typed record of the self census.
- `0064-selected-design-and-selection-authority.md` — G148 (NA-SELECTED-DESIGN, construction node M7): core::design (typed lifecycle, authority modes, principals, authority event, SelectedDesign with a digest identity over what it selects) and a validator against a verified container, the evidence for its candidate and a declared principal registry; a SELECTED design needs a registered human principal's event committing to it, a provider never selects, POLICY_AUTO is refused without a policy envelope; the registry ships empty, so selecting is the repository owner's decision; CLI design roots|propose|check.
- `0065-scoped-certificate-and-seal-policy.md` — G149 (NA-SEAL-POLICY, construction nodes M3 and M4): typed certificate blocker kinds (unknown codes refused); a scope policy disposes of every kind as REQUIRED_CLEAR, ACCEPTED_BOUNDED_RESIDUAL (enumerated or counted, with a reason) or OUT_OF_SCOPE, and permitted residuals stay listed; a SealPolicy with a digest identity binds it to the verification policy, HARD dimensions and the ELIGIBLE integrity verdict, and validate_policy refuses forbidden permissions; the declared strict self-scope policy finds the self census NOT_ELIGIBLE with its nine dynamic dependency classes the one permitted residual.
- `0066-candidate-designs-compared-by-measured-criteria.md` — G150 (NA-DESIGN-CANDIDATES): candidate designs at one coordinate over one verified container are compared by declared criteria that Atlas measures from the container's typed records (roots, per-function dimension records by distinct id, unresolved call sites); the comparison keeps the Pareto front as a set, names dominators, leaves unmeasurable designs out of competition, and ranks and selects nothing; a SELECTED design must cite a comparison of at least two measured candidates in which it is not dominated; CLI design candidates|compare, design check --comparison, design roots --path.
- `0067-full-oss-replay-and-capability-epochs.md` — G151 (FULL_OSS_REPLAY, replay R1 GitNexus): the owner-ordered replay lane over the deduplicated union of every donor ledger plus authorized donors (160 repositories), distinct from new-donor progression and frontier expansion; capability epochs (E0 at G150, E1 at G151) with verdicts valid per epoch; the REPLAY generation kind with pin, mission, differential, N-versus-N+1 and extinction evidence; cadence of at most one non-replay generation between replays and a one-donor window; E1: lifetime-erased impl shapes and an impact frontier that crosses closures, trait dispatch and uniquely named call sites as INFERRED.
- `0068-typescript-javascript-bounded-profile.md` — G152 (DEBT-MULTILANGUAGE, capability epoch E2): tree-sitter as syntax substrate only and Atlas's own `atlas.typescript.source-semantic.v1` extractor for TypeScript/JavaScript -- FUNCTION_IDENTITY, FUNCTION_SIGNATURE, SYMBOL (imports as declarations) and CALL with same-file lexical resolution of uniquely bound, never assigned module functions and constructors (DERIVED), resolved after the whole file is walked; every other dimension UNSUPPORTED; the same GitNexus pin goes from 363 to 65,414 functions and 6 to 21,045 resolved call edges.
- `0069-self-reconstruction-lane.md` — G153 (SELF_RECONSTRUCTION, owner directive): a permanent lane in which Atlas rebuilds bounded parts of itself from its census alone (SH0-SH6, Rust bootstrap target) -- construction IR v0 lifted from typed records with lineage, a construction input boundary without a source variant, typed construction gaps owned by debts, a never-guessing Rust backend, shadow workspaces under .atlas/.cache/shadow verified behaviorally against the compiled original and semantically by Atlas's census of the shadow, verdicts computed from evidence; Atlas chose core::donor::MaterializationMode; attempt 1 found the census did not know an enum from a struct, its derives, attributes or visibility, and SYMBOL definitions now carry their declared shape.
- `0070-foreign-functions-and-unrecognized-source.md` — G154 (FULL_OSS_REPLAY R3 tree-sitter, capability epoch E4): functions declared in extern blocks are FOREIGN_FUNCTION declarations with their ABI and no body, bound by the path resolver; a literal item-position include! splices its file into the including module (a computed one is not followed); inventory artifacts no frontend recognizes are an understanding gap by extension; on the tree-sitter pin 0 -> 165 foreign declarations and 2,133 -> 2,322 resolved call edges; tree-sitter decided EXTERNAL_BOUNDARY at E4.
- `0071-measured-support-levels.md` — G155 (NA-SUPPORT-LEVELS, universal engineering-world directive): every language and artifact class of a revision is placed on a measured ladder (L0 discovered, L1 identified, L2 syntax, L3 typed records, L4 obligations evaluated, L5 composed, L6 agent-usable) from its own inventory, census and world model; syntax is never semantics; claims above the measured level are refused and checked against roadmap/SUPPORT-LEVELS.toml; contracts/UNIVERSAL-ENGINEERING-WORLD.md fixes the architecture and its invariants and labels what is conceptual; DEBT-TARGET_PROFILE and DEBT-ARTIFACT_SEMANTICS opened.
- `0072-resource-dimension.md` — G157 (NA-RESOURCE-DIMENSION, DEBT-RESOURCE): RESOURCE is the thirteenth semantic dimension -- acquisitions of files, sockets, std lock guards and threads DERIVED from calls resolved to a declared std-path resource table; releases claimed only for a holder bound once by a `let` and never moved (end of block INFERRED, a resolved `drop` or `join` statement DERIVED); a dropped thread handle detaches and is never a release; temporaries, moved holders, FFI pairs and non-std resources keep an unknown release point.
- `0073-typescript-module-linking.md` — G158 (FULL_OSS_REPLAY R5 xyflow, capability epoch E7): a second CALL engine for TypeScript/JavaScript links modules -- named and default imports followed through exports, explicit re-exports and unambiguous star exports to module-level functions, across relative specifiers and workspace packages declaring a `source` entry; the same claim observed again, DERIVED; on the xyflow pin 147 -> 845 resolved call edges and getBezierPath 0 -> 10 callers (all ten TypeScript call sites); RESOURCE made canonical for every extractor.
- `0074-async-block-regions.md` — G159 (NA-ASYNC-REGIONS, DEBT-CALL, DEBT-EFFECT, DEBT-CONCURRENCY): an `async` block is its own executable region like a closure -- an ASYNC_BLOCK function `{async@line:column}` under `fn <enclosing region>`, every dimension of its body attributed to it, its calls resolved with the enclosing locals, enclosed by its defining function in composition; the enclosing function is never credited.
- `0075-compiled-package-entries.md` — G160 (FULL_OSS_REPLAY R6 GitNexus revalidation, capability epoch E9): a workspace package declaring only compiled entries (types, module, main, the `.` export) is entered at the one existing source its own tsconfig.json maps them back to through declared `outDir` -> `rootDir`; nothing without both directories, never when entries disagree; on the GitNexus pin 45,345 -> 45,746 resolved call edges, scoreImpactRisk 0 -> 10 callers, all correct.
- `0076-seal-gate-and-sealed-container.md` — G161 (NA-SEAL-GATE, DEBT-SEAL_GATE M8/M9, DEBT-VERIFICATION_EVIDENCE): one seal gate joins the seal policy, the container's own certificate (verdict recomputed), an ADMISSIBLE verification report and the required integrity verdict for the container's census digest and clean revision, and a SELECTED design with authority, comparison and verified identity; ELIGIBLE yields a SealRecord, NOT_ELIGIBLE every typed reason; the SEALED container (header flag SEALED, format minor 2, section 18 seal-record) is written only with a record that binds its certificate, census and revision and permits every blocker, and a sealed root is exactly its own canonical encoding; CLI seal gate, atlas seal.
- `0077-import-cycles-and-open-scope-causes.md` — G162 (FULL_OSS_REPLAY R7 zed, capability epoch E11): the import fixed point recognizes a period-2 oscillation (a placeholder travelling around a re-export cycle) and merges the alternating states conservatively instead of opening every module; every open scope records its cause (item macro, glob, module file, fixed point) and every withheld path call names it -- an `open scope: ` diagnostic per artifact, a component's `withheld`, GAP-OPEN-SCOPE and the unknowns lens; GAP-RESOURCE corrected; on a 17-crate slice of zed 7,648 -> 9,059 resolved INVOKES, 0 -> 6 resources, every file open -> 65 with causes; the whole workspace does not fit in 15 GB.
- `0078-weight-artifacts-physical-census-and-mechanical-construction.md` — G163 (owner-directed weight-artifact upgrade, NA-ARTIFACT-SEMANTICS-WEIGHTS, DEBT-ARTIFACT_SEMANTICS, capability epoch E12): weights as an artifact class with a physical layer (`atlas.weight-census.v1`: dtype, shape, payload byte range and BLAKE3 digest per tensor, ranges tiling the payload) apart from a semantic layer whose roles stay UNKNOWN; SafeTensors censused streamed and data-only (bounded header, duplicate keys and unknown fields refused, structure checked before payload); construction mechanical from the record and digest-verified ranges, header rebuilt, content preservation checked by re-census before the output is renamed into place; LOSSLESS / REVERSIBLE_WITH_RETAINED / LOSSY typed; the reference implementation reads the constructed output back equal; stages W2-W5 planned.
